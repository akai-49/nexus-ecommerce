{
  "name": "catalog-app-workspace",
  "version": "1.0.0",
  "description": "Enterprise Dual-Backend E-Commerce Workspace",
  "private": true,
  "scripts": {
    "install:all": "npm install && npm install --prefix shared && npm install --prefix backend-admin && npm install --prefix backend-store && npm install --prefix frontend",
    "sync-schemas": "bash shared/scripts/sync-schemas.sh",
    "db:up": "docker compose up -d",
    "db:down": "docker compose down",
    "dev:admin": "npm run start:dev --prefix backend-admin",
    "dev:store": "npm run start:dev --prefix backend-store",
    "dev:frontend": "npm run dev --prefix frontend",
    "build:all": "npm run build --prefix shared && npm run build --prefix backend-admin && npm run build --prefix backend-store && npm run build --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
